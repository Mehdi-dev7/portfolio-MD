[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA : toutes les routes renvoient index.html (utile si tu ajoutes du routing plus tard)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
